//Mualla Argin 728003004
/**
 * Exclusive-or gate:
 * out = not (a == b)
 */

CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
    Not(in=a,out=nota);
	Not(in=b,out=notb);
	And(a=nota,b=b,out=c);
	And(a=a,b=notb,out=d);
	Or(a=c,b=d,out=out);
}